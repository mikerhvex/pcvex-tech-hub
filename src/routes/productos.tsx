import { createFileRoute } from "@tanstack/react-router"; import { useState } from "react"; import { z } from "zod"; import { StoreLayout } from "@/components/store-layout"; import { ProductGrid } from "@/components/product-grid"; import { getProducts } from "@/lib/shopify"; import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from "@/components/ui/select";
export const Route=createFileRoute("/productos")({validateSearch:z.object({categoria:z.string().optional()}),loader:()=>getProducts(20),head:()=>({meta:[{title:"Productos | PcVex"},{name:"description",content:"Encontrá componentes y periféricos para tu PC."},{property:"og:title",content:"Productos | PcVex"},{property:"og:description",content:"Encontrá componentes y periféricos para tu PC."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:Products});
function Products(){const all=Route.useLoaderData();const{categoria}=Route.useSearch();const[sort,setSort]=useState("featured");let products=categoria?all.filter(p=>p.productType.toLowerCase()===categoria.toLowerCase()):all;products=[...products].sort((a,b)=>sort==="price-asc"?Number(a.priceRange.minVariantPrice.amount)-Number(b.priceRange.minVariantPrice.amount):sort==="price-desc"?Number(b.priceRange.minVariantPrice.amount)-Number(a.priceRange.minVariantPrice.amount):sort==="name"?a.title.localeCompare(b.title):0);return <StoreLayout><section className="mx-auto max-w-7xl px-4 py-14 sm:px-6 lg:px-8"><div className="grid grid-cols-[minmax(0,1fr)_auto] items-end gap-5"><div className="min-w-0"><p className="text-sm font-bold text-accent-blue">CATÁLOGO</p><h1 className="mt-2 truncate text-4xl font-bold">{categoria??"Productos"}</h1><p className="mt-3 text-muted-foreground">Encontrá componentes y periféricos para tu PC.</p></div><Select value={sort} onValueChange={setSort}><SelectTrigger className="w-44"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="featured">Destacados</SelectItem><SelectItem value="price-asc">Menor precio</SelectItem><SelectItem value="price-desc">Mayor precio</SelectItem><SelectItem value="name">Nombre</SelectItem></SelectContent></Select></div><div className="mt-12"><ProductGrid products={products}/></div></section></StoreLayout>}